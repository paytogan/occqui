function checkEntries(existingEntry, entry) {
  if (existingEntry === entry) {
    // Code to execute if the entries are strictly equal
    console.log('The entries are identical.');
  } else {
    // Code to execute if the entries are not strictly equal
    console.log('The entries are not identical.');
  }
}
